# Baked into the static build automatically by `vite build` (production
# mode). This is what makes the deployed frontend call the deployed
# backend instead of a relative "/api" path that doesn't exist once
# there's no dev server / proxy running.
VITE_API_BASE=https://backend-six-chi-11.vercel.app
